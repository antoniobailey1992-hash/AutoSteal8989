-- Delta Executor: AutoSteal8989 Private Hub
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
local Window = Library.CreateLib("PRIVATE HUB: AutoSteal8989", "Midnight")

-- PASSWORD PROTECTION
local Auth = Window:NewTab("Auth")
local AuthSec = Auth:NewSection("Enter Private Code")

AuthSec:NewTextBox("Enter Code", "Type AutoSteal8989 here", function(txt)
    if txt == "AutoSteal8989" then
        print("Access Granted")
        -- You can now access the other tabs
    else
        print("Access Denied")
    end
end)

-- STEALING TAB
local StealTab = Window:NewTab("Stealing")
local SSec = StealTab:NewSection("Roof-Drop Steal")

SSec:NewButton("Set Collection Spot", "Stand at your roof/pad", function()
    _G.DropPoint = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
end)

SSec:NewToggle("Auto-Snatch to Roof", "Teleports items to drop point", function(state)
    _G.Snatch = state
    while _G.Snatch do
        task.wait(0.5)
        pcall(function()
            if _G.DropPoint then
                for _, v in pairs(game.Workspace:GetChildren()) do
                    if v:IsA("Model") and (v.Name:find("Brainrot") or v.Name:find("Item")) then
                        local item = v.PrimaryPart or v:FindFirstChild("Handle")
                        if item then
                            -- Moves item to your roof so it drops down
                            item.CFrame = _G.DropPoint
                        end
                    end
                end
            end
        end)
    end
end)

-- LIVE TROLLING / FAKE ROBUX
local LiveTab = Window:NewTab("Live Trolling")
local LSec = LiveTab:NewSection("Fake Stats")

LSec:NewButton("Generate Fake Robux", "Chooses 50k - 1M randomly", function()
    local randomRobux = math.random(50000, 1000000)
    -- This changes the text on your screen (Visual Only for Live)
    pcall(function()
        local robuxLabel = game.Players.LocalPlayer.PlayerGui:FindFirstChild("RobuxLabel", true) 
        if robuxLabel then
            robuxLabel.Text = tostring(randomRobux)
        end
    end)
    print("Visual Robux set to: " .. randomRobux)
end)

LSec:NewButton("Fake Gift Admin Panel", "Sends fake chat message", function()
    local msg = "[SYSTEM]: User " .. game.Players.LocalPlayer.Name .. " has gifted Admin Panel to the server!"
    game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents.SayMessageRequest:FireServer(msg, "All")
end)

LSec:NewButton("Fake Gift Magic Carpet", "Sends fake chat message", function()
    local msg = "[SYSTEM]: User " .. game.Players.LocalPlayer.Name .. " has gifted Magic Carpet to the server!"
    game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents.SayMessageRequest:FireServer(msg, "All")
end)
